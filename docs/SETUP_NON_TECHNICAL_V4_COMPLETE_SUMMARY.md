# RepLife V4 Complete Setup Manual Summary

Use the full PDF/DOCX manual `RepLife_Automation_NonTechnical_Setup_Manual_v4_COMPLETE` together with this package.

Preferred zip: `replife-automation-content-ops-full-vps-v4-complete.zip`

The V4 manual preserves the full 66-page setup detail and integrates the V3 execution fixes retained in this package.

After upload and unzip, the key safe validation commands are:

```bash
cd ~/replife-backend
chmod +x scripts/*.sh
bash scripts/validate_all_scripts_preview.sh
bash scripts/day17m_strict_dry_run.sh
```

Do not continue to Day 18 or Day 19 if Day 17M fails.
