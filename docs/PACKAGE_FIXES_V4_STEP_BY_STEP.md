# V4 Step-by-Step Documentation Update

This package keeps the V4 automation scripts and full content-operations structure, then replaces the setup documentation with a clearer step-by-step guide. It preserves V3/V4 execution fixes: LF line endings, `.gitattributes`, `.venv/` ignored by Git, improved placeholder checks, corrected Day 17M inputs/arguments, and `scripts/day17m_strict_dry_run.sh`.
